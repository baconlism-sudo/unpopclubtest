# ต่อฟอร์มติดต่อเข้า Google Sheets

ใช้ **Google Apps Script** ไม่ใช่ Sheets API

**ทำไมเปลี่ยนวิธี** organization policy `iam.disableServiceAccountKeyCreation` บล็อกการสร้างคีย์ไว้ แก้ได้เฉพาะแอดมินของ organization วิธีนี้เลี่ยงไปเลย

**สิ่งที่ไม่ต้องมีอีกต่อไป** Cloud project · service account · คีย์ JSON · การ enable API

ใช้เวลาราว 10 นาที **ไม่มีค่าใช้จ่าย**

---

## ภาพรวมว่ามันทำงานยังไง

```
ฟอร์มบนเว็บ  →  Netlify Function  →  Apps Script  →  Google Sheet
                 ตรวจข้อมูล           เช็ครหัสลับ      เขียนแถวใหม่
                 ถือรหัสลับไว้
```

**ทำไมต้องผ่าน Netlify Function ไม่ยิงตรงจากเว็บ** เพราะ URL ของ Apps Script เป็นสาธารณะ ถ้าใส่ไว้ในหน้าเว็บ ใครเปิด view source ก็เห็น แล้วยิงข้อมูลขยะเข้ามาได้ไม่จำกัด

การให้ Function ถือรหัสลับไว้ฝั่ง server ทำให้ URL กับรหัสไม่เคยโผล่ในเบราว์เซอร์

---

## Step 1 · สร้าง Google Sheet

สร้าง Sheet ใหม่ใน Drive ของคุณ ตั้งชื่อไฟล์อะไรก็ได้

ใส่หัวคอลัมน์ที่ **แถวที่ 1** เรียงตามนี้

| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| Timestamp | Name | Email | Topic | Message | PDPA Consent | Source Page |

> **ลำดับต้องตรงเป๊ะ** ข้อความในหัวคอลัมน์เขียนว่าอะไรก็ได้ โค้ดไม่ได้อ่าน
> แต่ถ้าสลับลำดับ ข้อมูลจะลงผิดช่องโดยไม่มี error แจ้ง

**ชื่อแท็บ** ล่างซ้าย ปล่อยเป็น `Sheet1` จะง่ายที่สุด ถ้าเปลี่ยนต้องไปแก้ใน Code.gs ด้วย

---

## Step 2 · สร้างรหัสลับ

เปิดเทอร์มินัลแล้วรัน

```bash
openssl rand -hex 24
```

จะได้ข้อความยาวๆ มาแบบนี้ `a7810b20ae7fbe44d5e2a27533367a4c1455715c6065ad77`

**คัดลอกเก็บไว้ เดี๋ยวต้องใช้สองที่** ใน Code.gs และใน Netlify ต้องเป็นค่าเดียวกันเป๊ะ

ถ้าไม่มีเทอร์มินัล ใช้รหัสสุ่มยาวๆ อะไรก็ได้ที่ไม่มีใครเดาได้ ขอแค่ยาวเกิน 30 ตัวอักษร

---

## Step 3 · ติดตั้ง Apps Script

1. เปิด Google Sheet ที่สร้างไว้
2. เมนู **ส่วนขยาย** แล้วเลือก **Apps Script**
   ภาษาอังกฤษคือ **Extensions** แล้ว **Apps Script**
3. จะเปิดแท็บใหม่ที่มีไฟล์ `Code.gs` และมีโค้ดตัวอย่างอยู่
4. **ลบโค้ดเดิมทิ้งทั้งหมด** แล้ววางเนื้อหาจากไฟล์ `apps-script/Code.gs` ในโปรเจกต์นี้ลงไป
5. แก้บรรทัดนี้ ใส่รหัสลับจาก Step 2

   ```js
   const SECRET = 'REPLACE_WITH_YOUR_SECRET';
   ```

6. ถ้าเปลี่ยนชื่อแท็บ Sheet ให้แก้ `SHEET_TAB` ด้วย
7. กด **บันทึก** ไอคอนแผ่นดิสก์

---

## Step 4 · Deploy เป็น Web App

1. มุมขวาบนกด **ทำให้ใช้งานได้** หรือ **Deploy** แล้วเลือก **การทำให้ใช้งานได้ใหม่** หรือ **New deployment**
2. กดไอคอนเฟือง แล้วเลือกประเภทเป็น **เว็บแอป** หรือ **Web app**
3. ตั้งค่าสองอย่างนี้ให้ถูก **สำคัญมาก**

   | ช่อง | ต้องเลือก |
   |---|---|
   | ดำเนินการในฐานะ · Execute as | **ฉัน** หรือ **Me** |
   | ผู้ที่มีสิทธิ์เข้าถึง · Who has access | **ทุกคน** หรือ **Anyone** |

4. กด **ทำให้ใช้งานได้** หรือ **Deploy**
5. Google จะขออนุญาตเข้าถึง Sheet ครั้งแรก กด **ตรวจสอบสิทธิ์** แล้วเลือกบัญชีของคุณ

   > ถ้าขึ้นหน้าจอสีเหลืองว่า **Google ยังไม่ได้ยืนยันแอปนี้** กด **ขั้นสูง** แล้วกด **ไปที่ ... (ไม่ปลอดภัย)**
   > ไม่ได้อันตราย เพราะแอปนี้คือสคริปต์ที่คุณเพิ่งเขียนเอง Google แค่ยังไม่ได้รีวิว

6. **คัดลอก URL ที่ได้มา** หน้าตาแบบนี้ ลงท้ายด้วย `/exec`

   ```
   https://script.google.com/macros/s/AKfycb.................../exec
   ```

**ทดสอบเร็วๆ** เอา URL นั้นเปิดในเบราว์เซอร์ ถ้าเห็นข้อความแบบนี้แปลว่า deploy สำเร็จ

```json
{"ok":true,"service":"unpop contact receiver"}
```

---

## Step 5 · ตั้งค่า Netlify

**Site settings** แล้วเข้า **Environment variables** แล้วกด **Add a variable**

| Key | Value |
|---|---|
| `APPS_SCRIPT_URL` | URL จาก Step 4 ลงท้ายด้วย `/exec` |
| `APPS_SCRIPT_SECRET` | รหัสลับจาก Step 2 ต้องตรงกับใน Code.gs เป๊ะ |

**ลบตัวแปรเก่าทิ้งได้** ถ้าเคยตั้งไว้ `GOOGLE_SHEET_ID` · `GOOGLE_SHEET_TAB` · `GOOGLE_SERVICE_ACCOUNT_JSON` · `GOOGLE_CLIENT_EMAIL` · `GOOGLE_PRIVATE_KEY` โค้ดไม่ได้ใช้แล้ว

**เสร็จแล้วต้อง redeploy** ไม่งั้น function ยังไม่เห็นค่าใหม่
Deploys แล้วกด Trigger deploy แล้วเลือก Deploy site

---

## Step 6 · ทดสอบ

ส่งฟอร์มจากหน้า contact จริง

**ผ่าน** เด้งไปหน้า thank-you และมีแถวใหม่โผล่ใน Sheet ทันที

**ไม่ผ่าน** ขึ้นข้อความสีแดงใต้ปุ่ม ไปดู log ที่ **Netlify** แล้วเข้า **Functions** แล้วเลือก **contact**

| ข้อความใน log | สาเหตุ |
|---|---|
| `APPS_SCRIPT_URL หรือ APPS_SCRIPT_SECRET ยังไม่ได้ตั้ง` | ลืม Step 5 หรือใส่แล้วแต่ยังไม่ redeploy |
| `unauthorized` | รหัสลับสองที่ไม่ตรงกัน เช็ก Code.gs กับ Netlify |
| `sheet tab not found` | ชื่อแท็บใน `SHEET_TAB` ไม่ตรงกับชื่อจริงใน Sheet |
| `Apps Script ตอบกลับไม่ใช่ JSON` | ตั้ง Who has access ไม่เป็น Anyone หรือ URL ผิด |
| `missing required fields` | ฟอร์มส่งมาไม่ครบ ปกติไม่เกิดเพราะเบราว์เซอร์บังคับอยู่แล้ว |

---

## เวลาแก้โค้ดใน Apps Script ต้อง deploy ใหม่

**นี่คือจุดที่คนพลาดบ่อยที่สุดของวิธีนี้** แก้โค้ดแล้วกดบันทึกอย่างเดียวไม่พอ Web App ยังรันเวอร์ชันเก่าอยู่

ต้องไปที่ **Deploy** แล้ว **จัดการการทำให้ใช้งานได้** หรือ **Manage deployments** กดไอคอนดินสอ แล้วเปลี่ยน Version เป็น **เวอร์ชันใหม่** แล้วกด **Deploy**

ทำแบบนี้ URL จะไม่เปลี่ยน ไม่ต้องไปแก้ที่ Netlify

---

## เรื่อง PDPA

**คอลัมน์ PDPA Consent จะเป็น `yes` ทุกแถว** เพราะติ๊กช่องยินยอมแล้วเท่านั้นถึงส่งได้

เก็บไว้เป็นหลักฐานคู่กับ Timestamp ว่าเขายินยอมตอนไหน

**เวลามีคนขอให้ลบข้อมูล** ลบแถวนั้นใน Sheet ได้เลย จบ

---

## สิ่งที่ควรรู้เพิ่ม

**Timestamp เป็นเวลาไทยแล้ว** วิธีนี้แก้ปัญหาเดิมไปในตัว รูปแบบเป็น `2026-07-19 21:23:01` เรียงลำดับใน Sheet ได้ปกติ ถ้าอยากเปลี่ยนเขตเวลาแก้ที่ `TIMEZONE` ใน Code.gs

**โควตา Apps Script** บัญชี Gmail ทั่วไปได้ 20,000 การเรียกต่อวัน ฟอร์มติดต่อไม่มีทางชน

**ถ้ารหัสลับหลุด** สร้างใหม่ด้วย `openssl rand -hex 24` แล้วเปลี่ยนทั้งใน Code.gs และ Netlify จำไว้ว่าต้อง deploy Apps Script ใหม่ด้วย

**URL ของ Web App เป็นสาธารณะโดยธรรมชาติ** ใครรู้ URL ก็ยิง request เข้ามาได้ แต่ถ้าไม่มีรหัสลับที่ถูกต้อง จะโดนปฏิเสธทันทีและไม่มีอะไรถูกเขียนลง Sheet

**ถ้า Netlify Function ล่ม ข้อมูลจะหาย** ตอนนี้ยังไม่มีระบบสำรอง ถ้าอยากปลอดภัยขึ้นค่อยเพิ่มการส่งอีเมลแจ้งเตือนทีหลัง

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | ทำอะไร |
|---|---|
| `apps-script/Code.gs` | โค้ดที่ต้องเอาไปวางใน Apps Script |
| `form.js` | ดักการ submit แล้วยิง JSON ไปที่ function |
| `netlify/functions/contact.js` | ตรวจข้อมูล แล้วส่งต่อไป Apps Script พร้อมรหัสลับ |
| `api/contact.js` | ตัวเดียวกันแต่สำหรับ host อื่นที่ไม่ใช่ Netlify |
| `netlify.toml` | บอก Netlify ว่า function อยู่ที่ไหน |

**ห้ามใส่รหัสลับลงในไฟล์ที่ commit ขึ้น git** ให้อยู่ใน Netlify environment variables กับใน Apps Script เท่านั้น
